{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "22"
  },
  "steps": {
    "install": {
      "commands": [
        { "cmd": "npm install --omit=dev", "customName": "Install root dependencies" }
      ]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": []
    }
  },
  "deploy": {
    "startCommand": "node server/server.js",
    "inputs": [
      {
        "step": "install",
        "include": ["."]
      }
    ]
  }
}
