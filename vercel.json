{
  "version": 2,
  "projects": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    },
    {
      "src": "public_site",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "buildCommand": "npm install && npm run build"
      }
    },
    {
      "src": "admin_dashboard",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "buildCommand": "npm install && npm run build"
      }
    }
  ]
}
